PREFIX ond: <https://data.meemoo.be/terms/ond/>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>

SELECT ?id ?label 
WHERE {{
    ?id a skos:Concept;
    skos:prefLabel ?label;
    skos:inScheme ond:graad
}}